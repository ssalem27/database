#include "llist.h"



class llist{
    private:
        node *head;
        node *tail;
        int length;

    int insertNode(int index=-1, node *node){}
    node* deleteNode(char *key){}
    node* get(char *key){}
    int update(char *key, char *value){}

};
